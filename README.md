# Pumpkin-Seed-Classification-using-MLP
This project implements Deep learning MLP to classify pumpkin seeds based on extracted morphological features. A FastAPI web application is used to receive input data and return prediction results in JSON format.
